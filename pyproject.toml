[project]
name = "hbg-typesense-mcp"
version = "0.1.0"
description = "Minimal MCP server implementation for basic search in Typesense."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=2.0.0",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.15.0",
    "typesense>=2.0.0",
    "uvicorn>=0.52.4",
]
